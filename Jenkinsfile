pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building the application...'
                ls -la
            }
        }

        stage('Test') {
            steps {
                echo 'Pipeline is working!'
            }
        }
    }
}

